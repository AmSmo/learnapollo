query Doggos($limit: Int!, $cursor: String) {
  doggos(cursor: $cursor, limit: $limit) {
    hasMore
    doggos {
      id
      name
      ownerId
      createdDate
      updatedDate
      textSnippet
    }
  }
}
