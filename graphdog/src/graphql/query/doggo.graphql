query Doggo($id: Int!) {
  doggo(id: $id) {
    id
    name
    ownerId
    createdDate
    updatedDate
    textSnippet
    treats
    treatStatus
    story
    owner {
      id
      username
    }
  }
}
